#include <bits/stdc++.h>

using namespace std;

void solution()
{
    int n, k;
    cin >> n >> k;

    for (int i = 0; i < n; i++)
    {
        int a, b;
        cin >> a >> b;
    }

    cout << "비와이\n";
}

int main(void)
{
    ios::sync_with_stdio(false);

    cin.tie(NULL);
    cout.tie(NULL);

    solution();

    return 0;
}